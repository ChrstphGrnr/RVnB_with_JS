<div><p>Destination: <strong><%= @trip.name %> </strong></p></div>

RV Name: <strong><%= @trip.rv.name %></strong>
<br>
<p>Number of Guests: <strong><%= @trip.guests %></strong></p>

Start Date: <strong><%= @trip.start_date %></strong>  <br>
End Date: <strong><%= @trip.end_date %></strong>

<br>

<p>    Total Trip Price: <strong><%= @trip.total_price %></strong></p>

<br>

<button class="waves-effect wave-light teal lighten-3 btn-small" >
    <%= link_to "delete", {action: :destroy, id: @trip.id}, method: :delete, data: {confirm: "Are you sure?"} %>
    <i class="material-icons left">delete</i>
</button>
<button class="waves-effect wave-light teal lighten-3 btn-small">
    <%= link_to "edit", edit_user_trip_path(current_user, @trip) %>
    <i class="material-icons left">edit</i>
</button>

<!---
<%= form_tag user_trip_path(current_user.id, @trip.id), method: :delete %>
    <%= submit_tag "Delete" %> 